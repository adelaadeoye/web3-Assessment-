const basicCal = (a, b) => {
  console.log(
    `The Multiplication of ${a} and ${b} = ${a * b}\nThe Addition of ${a} and ${b} = ${
      a + b
    }\nThe Subtraction addition of ${a} and ${b} = ${
        a - b
      }\nThe Division addition of ${a} and ${b} = ${a/b}\n`
  );
};

basicCal(2, 4);
